# Print Method for summary.latent_G Objects

Print Method for summary.latent_G Objects

## Usage

``` r
# S3 method for class 'summary.latent_G'
print(x, digits = 4, ...)
```

## Arguments

- x:

  A `summary.latent_G` object from
  [`summary.latent_G()`](https://joonho112.github.io/IRTsimrel/reference/summary.latent_G.md).

- digits:

  Integer. Number of decimal places for printing.

- ...:

  Additional arguments (ignored).

## Value

The input object, invisibly.
