# Extract theta values from latent_G object

Convenience function to extract the theta vector for use with other
functions.

## Usage

``` r
# S3 method for class 'latent_G'
as.numeric(x)
```

## Arguments

- x:

  A `latent_G` object.

## Value

Numeric vector of latent abilities.
